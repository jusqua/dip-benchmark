[project]
name = "python-numpy-benchmark"
version = "0.1.0"
description = "Benchmark of basic image processing operations with Python and NumPy"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "opencv-python>=4.10.0.84",
]
